= simple_form_for @index, :html => { :multipart => true, :novalidate => true } do |f|
  = f.association @video do |video_form|
    = render "videos/form", :locals => {:f => video_form}
  = f.button :submit
