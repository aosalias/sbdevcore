= simple_form_for @index, :html => { :multipart => true, :novalidate => true } do |f|
  = f.association @photo do |photo_form|
    = render "photos/form", :locals => {:f => photo_form}
  = f.button :submit
